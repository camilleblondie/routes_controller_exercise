<h2>Listing products</h2>
<p>Number of products : <%= Product.count %></p>
<p>
<% if !Product.all.blank? %>
<ul>
  <% @products.each do |product| %>
    <li>
    Product Name: "<%= product.name %>", Cost: <%= product.price %>
    </li>
    <% end %>
</ul>
<% else %>
  There are no products yet.
<% end %>
</p>

<%= link_to 'New Product', products_new_path %><br>
